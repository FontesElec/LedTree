<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="max.android.develop.bluetooth_tree">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RandomColor"></activity>
        <activity android:name=".Firework" />
        <activity android:name=".Snakes" />
        <activity android:name=".Gradients" />
        <activity android:name=".FireStyles" />
        <activity android:name=".Seasons" />
        <activity android:name=".Themes" />
        <activity android:name=".BluetoothConnection" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ControlPanel"
            android:screenOrientation="portrait" />
    </application>

</manifest>